name: catfecito-staging

services:
  # Plantilla no operativa para staging.
  # Se activara cuando se defina estrategia de deploy de staging.
  placeholder:
    image: alpine:3.20
    command: ["sh", "-c", "echo 'Staging docker aun no habilitado'; sleep infinity"]
