/**
 * @typedef {Object} SelectOption
 * @property {string|number} value
 * @property {string} label
 */

/**
 * @typedef {Object} AsyncState
 * @property {boolean} isLoading
 * @property {string|null} error
 */

export {};

/**
 * Este archivo define los tipos relacionados con la interfaz de usuario para mejorar la autocompletación y documentación en el código.
 * Los tipos incluyen las opciones para componentes de selección y el estado de operaciones asíncronas.
 * No contiene lógica de negocio ni funciones, solo definiciones de tipos para uso en el proyecto.
 */
