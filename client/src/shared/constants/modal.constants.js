export const MODAL_IDS = Object.freeze({
  CART: "cart-modal",
  CONFIRM_DELETE: "confirm-delete-modal",
  IMAGE_PREVIEW: "image-preview-modal",
  AUTH_REQUIRED: "auth-required-modal",
});

export const MODAL_SIZES = Object.freeze({
  SM: "sm",
  MD: "md",
  LG: "lg",
  XL: "xl",
  FULL: "full",
});

export const MODAL_CLOSE_REASONS = Object.freeze({
  BACKDROP: "backdrop",
  ESCAPE: "escape",
  CLOSE_BUTTON: "close_button",
  ACTION: "action",
});

export const MODAL_ANIMATION_MS = Object.freeze({
  ENTER: 180,
  EXIT: 140,
});

/**
 * Estas constantes sirven para centralizar los identificadores, tamaños, razones de cierre y duraciones de animación de los modales.
 * Esto ayuda a evitar errores de tipeo y facilita el mantenimiento y la consistencia en toda la aplicación.
 */
