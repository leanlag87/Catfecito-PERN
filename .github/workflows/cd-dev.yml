name: CD Development - Disabled by default

# Estado actual: DESACTIVADO.
# Este workflow esta en modo manual y bloqueado por variable.
#
# Como activarlo:
# 1) Definir CATFECITO_ENABLE_CICD=true en variables del repo.
# 2) Cambiar on.workflow_dispatch por push a develop si quieres automatico.
# 3) Reemplazar los pasos de echo por deploy real.

on:
  workflow_dispatch:

jobs:
  activation-info:
    name: Activation info
    runs-on: ubuntu-latest
    steps:
      - name: CD Dev is disabled
        run: echo "CD Development desactivado por defecto."

  deploy-development:
    name: Deploy to Development
    # Desactivado por defecto. Cambiar a true para habilitar.
    if: ${{ false }}
    runs-on: ubuntu-latest
    # Al activar, agregar: environment: development
    steps:
      - uses: actions/checkout@v4
      - name: Placeholder deploy
        run: |
          echo "TODO: reemplazar este paso por deploy real a Development."
          echo "Ejemplo: serverless deploy --stage dev"
